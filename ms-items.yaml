spring:
  application:
    name: ms-items
#  cloud:
#    discovery:
#      client:
#        simple:
#          instances:
#            ms-products:
#              - uri: http://localhost:8001
#              - uri: http://localhost:9001
#              - uri: http://localhost:9002
#              - uri: http://localhost:9003

server:
  port: 9091

web:
  config:
    services:
      ms-product:
        base-url: http://ms-product

eureka:
  client:
    service-url:
      defaultZone: http://discovery-service-app:9000/eureka

resilience4j:
  circuitbreaker:
    configs:
      default:
        sliding-window-size: 6
        failure-rate-threshold: 50
        wait-duration-in-open-state: 20s
        permitted-number-of-calls-in-half-open-state: 5
        slow-call-duration-threshold: 2s
        slow-call-rate-threshold: 50
    instances:
      items:
        base-config: default
  timelimiter:
    configs:
      default:
        timeout-duration: 3s
    instances:
      items:
        base-config: default

config:
  message: config read from config server

management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: http://zipkin-service:9411/api/v2/spans



